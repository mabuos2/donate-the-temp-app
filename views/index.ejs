<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Donate the Temp Game</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <h1 style="text-align:center"><i>Donate the Temp Game</i></h1>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="city" type="text" class="ghost-input" placeholder="Enter any city name" required>
          <input type="submit" class="ghost-button" value="Get temperature and donate that amount!">
        </form>
        <% if(weather !== null){ %>
          <p><%= weather %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>